version: 2

models:
  - name: contracts_sfdc       # your model name
    description: "Contracts enriched with account info"
    columns:
    # --   - name: contract_id
    # --     description: "Unique identifier for the contract"
    # --     tests:
    # --       - not_null
    # --       - unique
      - name: account_id
        description: "Linked Salesforce account ID"
        tests:
          - not_null
    # --   - name: contract_number
    # --     description: "Salesforce contract number"
    # --     tests:
    # --       - not_null
    # --       - unique
